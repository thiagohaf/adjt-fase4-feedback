---
scope: Plataforma de Feedbacks FIAP — API + serverless AWS (Tech Challenge Fase 4)
purpose: build-substrate
altitude: feature
updated: 2026-07-20T00:54
---

- (event) Architecture Create Fast path a partir do PRD prd-adjt-fase4-2026-07-20; entregável principal = spine (build-substrate); renderings extras adiados ao finalize
- (constraint) PRD final: FR-1..17, NFRs §8, constraints §10, non-goals §5; stack e modelo cloud no addendum; OQ-3..5 do PRD abertas para arquitetura
- (constraint) Solo acadêmico; prazo 28/07/2026; região sa-east-1; custo ~USD 20-30/mês; teardown pós-entrega; sem UI; SES sandbox OK
- (decision) Paradigma = Modular Monolith Layered (API Spring) + Async Side Effects (SQS/EventBridge→Lambda). Binds: todo o sistema. Prevents: microserviços por domínio ou API Gateway+Lambda como host da API REST. Rule: capacidades síncronas vivem no monólito ECS; efeitos colaterais (alerta, relatório) só em Lambdas SRP
- (decision) Modelo cloud [ADOPTED addendum]: API em ECS Fargate; alerta e relatório em Lambda; S3 para PDF; SES para e-mail. Prevents: API só-Lambda ou MSK em prod
- (version) Spring Boot addendum=3.4.5; web-check 2026-07-20: linha 3.4 OSS EOL (último patch 3.4.13); suportados OSS: 4.0.7 / 4.1.0. Draft pin: Java 17 + Spring Boot 3.4.13 (mínimo da linha do addendum) com ASSUMPTION de upgrade opcional p/ 4.0.7
- (decision) Persistência: Avaliação e demais entidades de domínio owned pela API; escrita só via API. Lambdas não mutam Avaliação. Prevents: dois writers da mesma entidade
- (decision) Mutação Avaliação: persistir + calcular Urgência na mesma transação; se ALTA, publicar SQS após commit bem-sucedido. Falha de publish/SES não apaga Avaliação. Prevents: alerta sem persistência ou rollback por falha de e-mail
- (decision) Contrato evento alerta: mensagem SQS self-contained (avaliacaoId, descricao, urgencia, ocorridoEm, aulaId, cursoId). Lambda notificação não consulta RDS. Prevents: Lambda de alerta acoplada ao schema do banco
- (decision) Relatório: UMA Lambda parametrizada (periodo=diario|semanal); duas regras EventBridge. Lambda lê RDS para agregação e escreve PDF S3 + SES. Prevents: terceira Lambda 1:1 com job; Prevents: API gerando PDF no request path
- (decision) Mensageria: Kafka só Docker Compose local; produção AWS = SQS. Adapter de publicação abstrai o broker. Prevents: MSK em prod; Prevents: domínio importar cliente Kafka/SQS
- (decision) Auth: JWT com claim ESTUDANTE|ADMINISTRADOR; Spring Security na API; Lambdas não autenticam usuários finais. Prevents: papel só no cliente; Prevents: auth nas Lambdas
- (decision) Boundaries pacote API: api(web) → application → domain ← infrastructure. Dependência só para dentro. Prevents: domain→Spring Web; Prevents: controllers→JPA direto
- (decision) Demo relatório OQ-3: disparo manual via invoke Lambda no console AWS (sem endpoint admin no MVP). Prevents: cron-only blocking gravação; Prevents: endpoint extra
- (decision) API paths OQ-4: /api/v1/... recurso avaliacoes (ASCII). Prevents: mistura path acentuado sem versão
- (assumption) PDF OQ-5: tabelas com agregados obrigatórios, sem gráfico
- (assumption) Lambdas: Java 17 Corretto, handlers leves AWS SDK v2 sem Spring Boot na Lambda; shared DTOs via módulo lib se precisar
- (assumption) IaC: Terraform + GitHub Actions (ASSUMPTION); usuário pode trocar por CDK
- (assumption) RDS PostgreSQL db.t3.micro single-AZ sa-east-1; ECS 0.25 vCPU; secrets em Secrets Manager
- (direction) Greenfield: scaffold Spring Initializr (Web, Security, Data JPA, Validation, Actuator, Flyway) + módulos lambdas separados
- (question) Confirmar pin Spring Boot: 3.4.13 (EOL OSS, alinhado addendum) vs 4.0.7 suportado?
- (question) Confirmar IaC: Terraform vs CDK vs Console+scripts mínimos?
- (event) Draft ARCHITECTURE-SPINE.md escrito (Fast path); aguardando review do usuário antes do Reviewer Gate / finalize
- (decision) Spring Boot pin = 4.0.7 (usuário). Binds: API + Lambdas. Prevents: drift para 3.4.x EOL do addendum. Substitui draft 3.4.13
- (decision) IaC = AWS CDK (usuário). Binds: infra/. Prevents: Terraform vs CDK paralelo. Rule: toda infra AWS provisionada via CDK; GHA invoca cdk deploy
- (decision) Lambdas = Spring Boot + Spring Cloud Function adapter AWS (usuário). Binds: lambda-notification, lambda-report. Prevents: handlers 'plain SDK' divergindo do stack Spring. Rule: FunctionInvoker; sem Tomcat nas Lambdas; shaded/thin jar
- (decision) OQ-3/4/5 confirmados pelo usuário: AD-9, AD-10 e PDF tabular permanecem
- (decision) Qualidade: cobertura mínima unitária JaCoCo >= 90% no CI. Binds: api + lambdas. Prevents: módulos sem gate de cobertura. Rule: build falha se line coverage unitária < 90%; excluir só gerados/config bootstraps documentados
- (version) Spring Boot 4.0.7; Spring Cloud Function (BOM alinhado ao Boot 4 / SCF 5.x); JaCoCo plugin atual (0.8.x); AWS CDK 2.x
- (assumption) CDK language = TypeScript (seed); pode trocar para CDK Java sem mudar AD de IaC
- (event) Review do usuário aplicado; iniciando Finalize + Reviewer Gate
- (version) Pins verificados web 2026-07-20: Boot 4.0.7; Spring Cloud BOM 2025.1.2; SCF 5.0.3; JaCoCo 0.8.15; aws-cdk-lib 2.261.0
- (decision) AD-15 inscrição/unicidades; AD-16 read model relatório; AD-17 VPC/ALB/Lambda network; AD-18 Resilience4j+DLQ — autofix do Reviewer Gate
- (decision) AD-5/6/8/11/12 apertados: payload único Kafka=SQS; crons 08:00 SP; matriz auth FR-8; métricas+alarme; secrets canônicos; sizing ECS 0.25 + db.t3.micro
- (version) OpenPDF pin corrigido para 3.0.5 (web); Resilience4j 2.4.0 confirmado
- (event) Reviewer Gate: lint 0 findings; rubric/version/adversarial/reconcile aplicados; spine status final
- (event) spine finalized
